---
- name: main config
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - include: basics.yml
    - include: gendev.yml
    - include: feddev.yml
    - include: gitconfig.yml
